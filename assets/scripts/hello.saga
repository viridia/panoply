pub fn hello() -> i32 {
    assert(true);
    //println("Hello from SAGA");
    add(1) + 2 + (3.0 as i32)
}

fn add(arg: i32) -> i32 {
    let c = 1;
    arg + c
}

fn name() {
//    debug("Hello from SAGA")
}

// native fn debug(s: String);
native fn assert(condition: bool);
